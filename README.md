# firebase-web-push-notifications-example
This is an example of how to configure a web page to receive web notifications with firebase 9.8.1

This example shows how to enable a page to receive web notifications in the foreground and in the background (by the service worker) with firebase
